import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#1DB954',
        },
        secondary: {
          main: '#191414',
        },
      },
});

export default theme;